spring.datasource.url=jdbc:h2:mem:prgymdb
spring.datasource.driverClassName=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=

spring.h2.console.enabled=true
spring.jpa.hibernate.ddl-auto=create-drop

# Nunca commitar um valor real aqui. Defina a variável de ambiente JWT_SECRET
# antes de subir a aplicacao (veja .env.example). Sem ela, o boot falha de proposito.
jwt.secret=${JWT_SECRET:asf7iu6vnqo7d3slv1mo}

app.frontend-url=http://localhost:8080

spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=${MAIL_USERNAME:}
spring.mail.password=${MAIL_PASSWORD:}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true